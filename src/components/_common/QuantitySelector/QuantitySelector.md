QuantitySelector:

```tsx
<QuantitySelector
  value={0}
  onDecrement={(v) => console.log(v)}
  onIncrement={(v) => console.log(v)}
  size="small"
  disabled
/>
```

```tsx
<QuantitySelector
  value={0}
  onDecrement={(v) => console.log(v)}
  onIncrement={(v) => console.log(v)}
  size="large"
/>
```
